
{
    "name": "Toolchain Workspace",
    "dockerFile": "../Dockerfile",
    "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind",
    "workspaceFolder": "/workspace",

 
    "initializeCommand": "xhost +local:docker || xhost +local:root || true",

    "runArgs": [
        "--privileged"
    ],

    "mounts": [
            {
            "source": "/dev/bus/usb",
            "target": "/dev/bus/usb",
            "type": "bind"
        }
    ],

    // Download and extract the custom packaged Tools 
    "postCreateCommand": "if [ ! -d \"/workspace/Tools\" ]; then echo '[*] Downloading tools...'; curl -f -L \"https://github.com/Sbustamantem/Femto_Risc-V_SoC/releases/download/v0.2.0-alpha/Tools.tar.xz\" | tar -xJf - -C /workspace/; else echo '[✓] Tools already exist.'; fi",

 


    "customizations": {
        "vscode": {
            "extensions": [
                "spencerwmiles.vscode-task-buttons"   
            ]
        }
    }
}